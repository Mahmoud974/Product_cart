/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ["dpcsh77th2fy8.cloudfront.net", "d30997lbsy3es6.cloudfront.net"], // Ajoute ce domaine aussi
  },
};

export default nextConfig;
